<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <!-- Always include this permission -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" /> <!-- Include only if your app benefits from precise location access. -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <!--
 Required only when requesting background location access on
      Android 10 (API level 29) and higher.
    -->
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Restaurantealfredito"
        tools:targetApi="31">

        <activity
            android:name=".Mensajeria"
            android:exported="false" />
        <activity
            android:name=".PPTarjeta"
            android:exported="false" />
        <activity
            android:name=".PPBilletera"
            android:exported="false" />
        <activity
            android:name=".PPEfectivo"
            android:exported="false" />
        <activity
            android:name=".PasarelaPagos"
            android:exported="false" />
        <!--
             TODO: Before you run your application, you need a Google Maps API key.

             To get one, follow the directions here:

                https://developers.google.com/maps/documentation/android-sdk/get-api-key

             Once you have your API key (it starts with "AIza"), define a new property in your
             project's local.properties file (e.g. MAPS_API_KEY=Aiza...), and replace the
             "YOUR_API_KEY" string in this file with "${MAPS_API_KEY}".
        -->
        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />


        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_key" />

        <activity
            android:name=".actiivty_Google_Map"
            android:exported="false"
            android:label="@string/title_activity_actiivty_google_map" />
        <activity
            android:name=".activity_registro_usuario"
            android:exported="false" />
        <activity
            android:name=".antivity_iniciar1"
            android:exported="false" />
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.Restaurantealfredito.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Actividades.MotorizadoMenuActivity"
            android:exported="false"
            android:label="@string/title_activity_motorizado"
            android:theme="@style/Theme.Restaurantealfredito.NoActionBar" />
        <activity
            android:name=".Actividades.AdministradorMenuActivity"
            android:exported="false"
            android:label="@string/title_activity_administrador_menu"
            android:theme="@style/Theme.Restaurantealfredito.NoActionBar" />
        <activity
            android:name=".Actividades.ClienteMenuActivity"
            android:exported="false"
            android:label="@string/title_activity_cliente_menu"
            android:theme="@style/Theme.Restaurantealfredito.NoActionBar" />
    </application>

</manifest>